---
windows:
  sample-app:
    panes:
      - command: 'echo "I am a command"'
        path: '~/go'
      - command: 'which vim'
        path: '~/go'
      # - command: 'echo "I am a command"'
      #   path: '~/go'
      # - command: 'which vim'
      #   path: '~/go'
